# Dépendances de test uniquement, réinstallables par `npm install`.
node_modules/

# Rien de ce qui touche à un vrai coffre ne doit entrer dans l'historique.
*.kdbx
coffre-*.kdbx

# Sorties d'outils et fichiers temporaires.
graphify-out/
*.log
.DS_Store
Thumbs.db

# Notes de session périmées, doublon de REPRISE.md. Conservées sur le disque
# mais hors du dépôt : elles contiennent une phrase de test et n'ont rien à
# faire dans un dépôt public.
reprise.md MySafer.txt
